/**
 * Global AI feature flag.
 * Set to `false` to disable all AI calls across the entire app.
 * No AI credits will be consumed and the system runs fully offline.
 */
export const AI_ENABLED = false;
